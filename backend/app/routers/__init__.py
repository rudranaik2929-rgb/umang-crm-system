"""Modular API routers (Phase 2 incremental extraction from legacy_core)."""


def register_routers(api_router) -> None:
    """Attach routers as they are split out of legacy_core. No-op until migrated."""
    _ = api_router
